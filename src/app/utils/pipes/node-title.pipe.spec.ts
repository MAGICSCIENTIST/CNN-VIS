import { NodeTitlePipe } from './node-title.pipe';

describe('NodeTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NodeTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
