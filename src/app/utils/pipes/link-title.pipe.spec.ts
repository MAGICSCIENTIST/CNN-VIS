import { LinkTitlePipe } from './link-title.pipe';

describe('LinkTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
